# 仓库 = 工作区根，同时也是可发布到 GitHub 的 skill 仓库（Shenlun-SKILL）。
# 原则：skill 本体（.dsh/skills/shenlun-judge/）必须入库；个人数据、机构资料、真题原卷不入库。
# 说明：gitignore 只支持整行注释，# 后面跟在同一行会被当成模式的一部分，故注释一律独立成行。

# ---- 依赖与构建垃圾 ----
# npm 依赖，可由 package.json 还原，不入库
node_modules/

# npm pack 产物（skill 的打包副本），体积大且可重建
*.tgz

# ---- 缓存 ----
# MiKTeX 本地缓存，体积大、可重建
.miktex/

# Python 字节码缓存，可重建（只排除 skill 脚本的缓存，.dsh/ 本体仍入库）
.dsh/skills/shenlun-judge/scripts/__pycache__/
__pycache__/
*.pyc

# ---- 个人作答（隐私）----
# 本人作答原文，属个人隐私，不进公开仓库
作答记录/

# ---- 批改产出（个人数据）----
# 针对本人的批改结果与分数留痕，属个人数据
批改报告/

# 依个人作答生成的批改规范，属个人数据
批改规范/

# ---- 机构资料与真题原卷（版权）----
# 机构解析/参考答案，第三方版权，禁止再分发
参考答案/

# 真题原卷与题面整理，第三方版权，且体积大
真题/

# ---- 临时草稿 ----
# 本地待办/修改清单（用户随手记的改动要求），不属于仓库内容
新的修改.txt
readme修改.txt
*修改.txt
# 注：README 用的图片放 assets/，需要入库，因此不在此忽略

