# ============================================================================
# 仓库 = 工作区根，同时也是发布到 GitHub 的 skill 仓库（Shenlun-SKILL）
# 原则：skill 本体（.dsh/skills/shenlun-judge/）与配套脚本（工具/）必须入库；
#       个人数据、机构资料、真题原卷不入库。
# 说明：gitignore 只支持整行注释（# 后面跟内容会被当成模式的一部分），故注释一律独立成行。
# ============================================================================

# ---- 依赖与构建垃圾 ----
# npm 依赖，可由 package.json 还原，不入库
node_modules/

# npm pack 产物（skill 的打包副本），体积大且可重建
*.tgz
package-lock.json

# ---- 编辑器 / 系统垃圾 ----
# JetBrains（PyCharm/IDEA）工程配置，因人而异
.idea/
.vscode/
.DS_Store
Thumbs.db
desktop.ini

# MiKTeX 本地缓存，体积大、可重建（本机 LaTeX 日志目录，与申论无关）
.miktex/

# Python 字节码缓存，可重建
__pycache__/
*.pyc
*.pyo

# ============================================================================
# 以下为「按需保留」的排除项。
# 这些都是**个人数据或第三方版权内容**：默认不入公开仓库 / npm 包。
# 如果你确实想把某一类放进版本库（例如自建真题库想公开），
# 把对应那一行前面加 # 注释掉即可（但请自行确认版权与隐私风险）。
# ============================================================================

# ---- 个人作答（隐私）----
# 本人作答原文，属个人隐私
作答记录/

# ---- 批改产出（个人数据）----
# 针对本人的批改结果、分数与字数留痕，属个人数据
批改报告/

# ---- 依个人材料整理的规范（个人整理）----
# 多为依个人作答/范文整理的批改规范
批改规范/

# ---- 机构资料（第三方版权）----
# 机构解析 / 参考答案，版权归各机构，禁止再分发
参考答案/

# ---- 真题原卷（第三方版权，且体积大）----
# 真题 PDF 与题面整理，版权归命题机构，且体积很大
真题/

# ---- 临时草稿 ----
# 本地待办 / 修改清单，不属于仓库内容
新的修改.txt
readme修改.txt
*修改.txt
*修改.md

# ---- 本地随意导出的成品（如导出的 PDF 副本）----
# 注意：仓库根目录下任何 README.* 都会被 npm 强制打进包里，导出物请勿命名为 README.*
/README.pdf
/README-*.pdf
