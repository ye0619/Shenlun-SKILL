# ============================================================================
# 仓库 = 工作区根，同时也是发布到 GitHub 的 skill 仓库（Shenlun-SKILL）
# 原则：skill 本体（.dsh/skills/shenlun-judge/）与配套脚本（工具/）必须入库；
#       个人数据、机构资料、真题原卷不入库。
# 说明：gitignore 只支持整行注释（# 后面跟内容会被当成模式的一部分），故注释一律独立成行。
# ============================================================================

# ---- 依赖与构建垃圾 ----
# npm 依赖，可由 package.json 还原，不入库
node_modules/

# npm pack 产物（skill 的打包副本），体积大且可重建
*.tgz
package-lock.json

# ---- 编辑器 / 系统垃圾 ----
# JetBrains（PyCharm/IDEA）工程配置，因人而异
.idea/
.vscode/
.DS_Store
Thumbs.db
desktop.ini

# MiKTeX 本地缓存，体积大、可重建（本机 LaTeX 日志目录，与申论无关）
.miktex/

# Python 字节码缓存，可重建
__pycache__/
*.pyc
*.pyo

# ============================================================================
# 五个材料目录（2026-09-22 用户裁定）：
#   **只上传"框架"，不上传内容** —— 目的是让用户 clone 后直接得到目录结构与说明文件，
#   再往里放自己的材料。做法：忽略目录下全部内容，只放行 README.md / .gitkeep；
#   `真题/` 额外放行 **`国考/` 下的 .md**（作者整理后添加的入库题面）。
# ============================================================================

# ---- 个人作答（隐私）：只留框架 ----
作答记录/**
!作答记录/README.md
!作答记录/.gitkeep

# ---- 批改产出（个人数据）：只留框架 ----
批改报告/**
!批改报告/README.md
!批改报告/.gitkeep

# ---- 批改规范（依个人材料整理）：只留框架 ----
批改规范/**
!批改规范/README.md
!批改规范/.gitkeep

# ---- 机构资料（第三方版权）：只留框架 ----
参考答案/**
!参考答案/README.md
!参考答案/.gitkeep

# ---- 真题：只留框架 + 国考下的 md（PDF 与其它省份不入库）----
真题/*
!真题/README.md
!真题/.gitkeep
!真题/国考/
真题/国考/*
!真题/国考/.gitkeep
!真题/国考/*.md

# ---- 临时草稿 ----
# 本地待办 / 修改清单，不属于仓库内容
新的修改.txt
readme修改.txt
*修改.txt
*修改.md
*更新.txt

# ---- 评分规范的来源底稿（第三方内容，本地保留）----
# 从网络收集的国考评分规范原文，仅作 skill 修改依据，不入库
国考评分规范*.md
国考评分规范*.txt

# ---- 本地随意导出的成品（如导出的 PDF 副本）----
# 注意：仓库根目录下任何 README.* 都会被 npm 强制打进包里，导出物请勿命名为 README.*
/README.pdf
/README-*.pdf
